import java.awt.GridLayout;

import javax.swing.JPanel;

public class CartePlateau extends JPanel {
	JPanel plateauPicture = new JPanel();
	
	CartePlateau() {/*
		plateauPicture.setSize(plateau.xmax, plateau.ymax);
		// controleur --> getSizeDuPlateau ? ;
		plateauPicture.setLayout( new GridLayout());
		// if Controleur communique le nombre d'animaux et les met sur le plateau:
		//for (int=1; i<nbAnimaux; i++) {
		//	AnimalPicture ap = AnimalPicture ("animal.png");
		//	plateau.add(ap);}
		
		//apres les bloques:
		for (int i = 0; i< plateauPicture.xmax; i++) {
			for (int j = 0; j < plateauPicture.ymax; j++) {
				BlocPicture bp = new BlocPicture(i, j);
				//bp.setColor (controleur communique la couleur du bloc);
				plateauPicture.add(bp);
				plateauPicture.revalidate();
				plateauPicture.repaint();
				j = j + 10;
			}
			i = i + 10;
		}*/
			
	}
}
